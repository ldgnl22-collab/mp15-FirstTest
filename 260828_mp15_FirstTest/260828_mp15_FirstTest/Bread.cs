

public class Bread : Menu
{
    public Bread(int lotNumber, string name, int price, int type)
    {
        LotNumber = lotNumber;
        Name = name;
        Price = price;
        Type = type;
    }
}