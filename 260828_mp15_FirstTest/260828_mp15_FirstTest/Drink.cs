

public class Drink : Menu
{
    public Drink(int lotNumber, string name, int price, int type)
    {
        LotNumber = lotNumber;
        Name = name;
        Price = price;
        Type = type;
    }
}